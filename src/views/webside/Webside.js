import ReactDOM from 'react-dom/client';
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import Header from '../../components/header';

const Webside = () => {
    return (
        <div>
            <Header title={'Webside'} />
        </div>
    );
}
export default Webside;